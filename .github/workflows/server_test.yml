name: Server Test

on:
  push:
    paths:
      - 'server_example/**'
      - 'server_test/**'

jobs:
  publish:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - name: Build and Test
      run: make test_example_server
